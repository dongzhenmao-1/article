# article
well

实数 $a,b,c$ 满足 $a^2 + b^2 + c^2 = 1$，求 $(a-b)(b-c)(c-a)(a+b+c)$ 的最大值。这是一个问题，你可以先输出你的思路，我不会看（这样可以节省你之后的思考时间）我接下来会给出我的解题思路，我会一个一个步骤走，你在这个基础上给我一点提示（），可以吗？如果我说的有问题，也可以稍微引导我让我自己推出问题，尽量不要直接让我知道我有问题。当然如果我说对了，你也不要告诉我这一步我对了。



如果我说的有问题，也可以稍微引导我让我自己推出问题，尽量不要直接让我知道我有问题。当然如果我说对了，你也不要告诉我这一步我对了。

$a > c > 0 > b$

或许能够配点系数消成常数呢？可是前面的每一项系数之和都是 $0$。

如果大小关系正确的话，那么不妨令 $b' = -b$。

然后大小关系变成 $a > c > 0,\ b' > 0$

那么 $(a + b')(b' + c)(a - c)(a - b' + c)$，虽然破坏了轮换的结构，但至少每一项都是正的了。

观察结构，$a$ 是特殊的，$b$ 和 $c$ 是性质相反的，我决定重新排序：

$(b' + c) \times (a + b')(a - c)(a + c - b')$

但是非线性的约束条件也太烦了。

然后由于 $b, c$ 的内在联系，我想 $d = b' - c$，

然后：

$(2c + d) \times (a + c + d)(a - c)(a - d)$

然后知道如果 $b'+c$ 是定值，那么 $d\not = 0$，但也说不定，因为 $a = \sqrt{1 - b ^ 2 - c ^ 2}$

$d$ 不为零会让 $a$ 变小。

---


如果说 $b' = c$，那么 $a^2 + 2c^2 = 1,\ 2c(a+ c)(a - c)a$